services:
  app:
    build: .
    container_name: express_app
    restart: always
    ports:
      - "4000:3000"
    env_file:
      - .env
    networks:
      - app-network
    extra_hosts:
      - "host.docker.internal:host-gateway"

networks:
  app-network:
    driver: bridge
